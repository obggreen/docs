---
title: "Generate Image"
description: "Create images from text prompts using Nano Banana (Gemini)"
---

<Info>
  **Base URL:** `https://platform.runblob.io`
</Info>

## POST /v1/gemini/generate

Creates an image generation task from text description using Nano Banana (Gemini Image Generation).

<ParamField path="prompt" type="string" required>
  Text description of the image (1-4000 characters)
</ParamField>

<ParamField path="model" type="string" default="standard">
  Model version: `"standard"` or `"pro"`
</ParamField>

<ParamField path="quality" type="string" default="standard">
  Image quality (only for `pro` model): `"standard"`, `"2k"`, or `"4k"`
</ParamField>

<ParamField path="aspect_ratio" type="string" default="auto">
  Image aspect ratio. Options:
  - `"auto"` - automatic
  - **Landscape:** `"21:9"`, `"16:9"`, `"4:3"`, `"3:2"`
  - **Square:** `"1:1"`
  - **Portrait:** `"9:16"`, `"3:4"`, `"2:3"`
  - **Flexible:** `"5:4"`, `"4:5"`
</ParamField>

<ParamField path="temperature" type="number" default="0.7">
  Creativity level from 0 to 1. Higher values = more creative results.
</ParamField>

<ParamField path="images" type="array">
  Array of up to 4 input images (base64 or URLs) for image editing/transformation
</ParamField>

<ParamField path="callback_url" type="string">
  Webhook URL for completion notifications
</ParamField>

<CodeGroup>

```bash Text-to-Image
curl -X POST https://platform.runblob.io/v1/gemini/generate \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "A cute orange cat sitting on a windowsill, golden hour lighting",
    "model": "standard",
    "aspect_ratio": "16:9"
  }'
```

```bash Pro Model with 4K
curl -X POST https://platform.runblob.io/v1/gemini/generate \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Hyperrealistic portrait of a woman in cyberpunk style",
    "model": "pro",
    "quality": "4k",
    "aspect_ratio": "9:16",
    "temperature": 0.8
  }'
```

```bash Image Editing
curl -X POST https://platform.runblob.io/v1/gemini/generate \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Transform this photo into anime style",
    "model": "pro",
    "images": ["data:image/jpeg;base64,/9j/4AAQSkZJRg..."]
  }'
```

```python Python
import requests

# Text-to-Image
response = requests.post(
    "https://platform.runblob.io/v1/gemini/generate",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "prompt": "A cute orange cat sitting on a windowsill",
        "model": "standard",
        "aspect_ratio": "16:9"
    }
)

# Pro with 4K quality
response = requests.post(
    "https://platform.runblob.io/v1/gemini/generate",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "prompt": "Hyperrealistic portrait",
        "model": "pro",
        "quality": "4k",
        "temperature": 0.8
    }
)
```

```javascript JavaScript
// Text-to-Image
const response = await fetch('https://platform.runblob.io/v1/gemini/generate', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    prompt: 'A cute orange cat sitting on a windowsill',
    model: 'standard',
    aspect_ratio: '16:9'
  })
});

const data = await response.json();
console.log(data.task_uuid);
```

</CodeGroup>

### Response

```json
{
  "task_uuid": "ffd473d5-5bef-4e14-bf22-8d559be3c19f",
  "status": "pending",
  "price": "0.0200"
}
```

<ResponseField name="task_uuid" type="string">
  UUID of the generation task for status checking
</ResponseField>

<ResponseField name="status" type="string">
  Initial status, always `"pending"`
</ResponseField>

<ResponseField name="price" type="string">
  Amount charged in USD
</ResponseField>

<Note>
  Image generation typically takes 7-30 seconds. Use the `task_uuid` to check status or set up webhooks for notifications.
</Note>

<Warning>
  `quality` parameter (`2k`, `4k`) is only available for the `pro` model. Using it with `standard` model will result in an error.
</Warning>

## Input Images

You can provide up to **4 input images** for image editing or transformation:

- **Base64:** `"data:image/jpeg;base64,/9j/4AAQ..."`
- **URL:** `"https://example.com/image.jpg"`
- **Max size:** 20 MB per image
- **Formats:** JPEG, PNG, WEBP

```json
{
  "prompt": "Combine these images into a collage",
  "images": [
    "data:image/jpeg;base64,/9j/4AAQSkZJRg...",
    "https://example.com/image2.jpg",
    "data:image/png;base64,iVBORw0KGgo..."
  ]
}
```

