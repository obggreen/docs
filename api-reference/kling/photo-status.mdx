---
title: "Check Status"
description: "Monitor image generation progress"
---

## GET /v1/kling/o1-photo/generations/{generation_id}

Check the current status of your image generation task.

<ParamField path="generation_id" type="string" required>
  Generation UUID returned from the generate endpoint
</ParamField>

<Accordion title="Status Values">
  <ResponseField name="pending" type="string">
    Task is in queue waiting to be processed
  </ResponseField>
  <ResponseField name="processing" type="string">
    Image is currently being generated
  </ResponseField>
  <ResponseField name="completed" type="string">
    Image generation completed successfully
  </ResponseField>
  <ResponseField name="failed" type="string">
    Image generation failed with error
  </ResponseField>
</Accordion>

<CodeGroup>

```bash cURL
curl -X GET https://platform.runblob.io/v1/kling/o1-photo/generations/8814c065-88b5-4074-bfcc-32e2d295b2b9 \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```python Python
import requests

response = requests.get(
    "https://platform.runblob.io/v1/kling/o1-photo/generations/8814c065-88b5-4074-bfcc-32e2d295b2b9",
    headers={"Authorization": "Bearer YOUR_API_KEY"}
)
```

```javascript JavaScript
const response = await fetch(
  'https://platform.runblob.io/v1/kling/o1-photo/generations/8814c065-88b5-4074-bfcc-32e2d295b2b9',
  {
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY'
    }
  }
);

const data = await response.json();
console.log(data);
```

</CodeGroup>

<Tabs>
  <Tab title="Completed">
    ```json
    {
      "generation_id": "photo-550e8400-e29b-41d4-a716",
      "status": "completed",
      "prompt": "A futuristic city with flying cars...",
      "image_url": "https://cdn.example.com/photos/photo-550e8400.png",
      "model": "kling-o1-photo"
    }
    ```
  </Tab>
  <Tab title="Failed">
    ```json
    {
      "generation_id": "photo-550e8400-e29b-41d4-a716",
      "status": "failed",
      "image_url": null,
      "model": "kling-o1-photo",
      "message": "CONTENT_POLICY_VIOLATION"
    }
    ```
  </Tab>
  <Tab title="Processing">
    ```json
    {
      "generation_id": "photo-550e8400-e29b-41d4-a716",
      "status": "processing",
      "model": "kling-o1-photo"
    }
    ```
  </Tab>
</Tabs>

<Warning>
  Tasks timeout after 15 minutes. Failed tasks return specific error codes in the `message` field.
</Warning>

